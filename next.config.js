/** @type {import('next').NextConfig} */
module.exports = {
    images: {
        domains: ["localhost"],
    },
};

/* 
asi estaba


* @type {import('next').NextConfig}
const nextConfig = {
    reactStrictMode: true,
};

module.exports = nextConfig;


*/
